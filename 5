#include "Cell.h"

Cell::Cell(const Math::Vector<int>& cellPosition,
           const Math::Vector<float>& vertexPosition) 
        : cellPosition(cellPosition)
        , vertexPosition(vertexPosition) {}
