#pragma once

#include "Vector.h"

using namespace White::Util;

struct Cell {
    Cell(const Math::Vector<int>& cellPosition = {0, 0}, 
         const Math::Vector<float>& vertexPosition = {0.0f, 0.0f});

    Math::Vector<int> cellPosition;
    Math::Vector<float> vertexPosition; 
};
